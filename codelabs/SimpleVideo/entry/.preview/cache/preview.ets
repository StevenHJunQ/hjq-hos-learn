struct preview{build(){ListItem() {
            HorizontalItem({
              imageSrc: item.image,
              source: 'videoTest.mp4',
              videoName: item.name
            })
          }}}